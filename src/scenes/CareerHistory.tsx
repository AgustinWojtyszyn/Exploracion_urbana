import { clubById,type CareerState } from '../world/Architecture'
import { ClubCrest } from '../components/ClubIdentity'
import { Trophy } from '../games/shared/FootballVisuals'
export function CareerHistory({state}:{state:CareerState}){return <section className="career-history"><header><span className="eyebrow">EL ARCHIVO DE {state.playerName.toUpperCase()}</span><h2>Cada temporada, una huella.</h2><span>{state.history.length} CAPÍTULOS</span></header>{!state.history.length?<div className="empty-state"><strong>Tu historia está por empezar.</strong><span>Jugá tu primera temporada.</span></div>:<ol>{[...state.history].reverse().map(record=><li key={record.season}><span className="career-history__year">{String(record.season).padStart(2,'0')}</span><article><header><ClubCrest name={clubById(record.clubId).name}/><div><span>{record.age} AÑOS</span><h3>{clubById(record.clubId).name}</h3></div><b>{record.rating}<small>RATING</small></b></header><div className="career-history__numbers"><span>{record.matches} PJ</span><span>{record.goals} G</span><span>{record.assists} A</span>{record.overallAfter!==undefined&&<span>OVR {record.overallAfter}</span>}</div>{record.titles>0&&<div className="career-history__trophy"><Trophy/><strong>{record.competition}</strong></div>}<p>{record.note}</p></article></li>)}</ol>}</section>}
